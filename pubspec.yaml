name: tap_master_pro
description: A modern tap challenge game
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^5.2.1

flutter:
  uses-material-design: true

  assets:
    - assets/tap.wav
